# eGZ-Board 产品需求文档（PRD）

> 本文档是唯一权威的产品需求文档。**只讲「产品要做什么、页面长什么样、怎么操作、边界和规则」**。
> 不涉及任何技术实现、接口设计、数据库结构——那些由开发方自行设计。
> 所有功能、页面布局、交互流程、异常状态、权限边界都必须写清楚到可直接开发的程度。

---

## 0. 产品概述

**eGZ-Board 是一套「电子班牌」系统**：教室/走廊/校门口的电子大屏全屏显示信息壁纸，管理员通过网页后台管理大屏上展示的内容。

目标客户：学校（K12 / 高中）。产品需**通用化**，可服务多所学校、多年级、多班级、不同地区，不绑定任何单一学校或地区。

### 两个客户端

| 端 | 说明 |
| --- | --- |
| **展示端**（大屏） | 教室内的 Windows 大屏、校门口/走廊竖屏等。开机即全屏展示，无需任何操作。 |
| **管理端**（网页后台） | 老师/管理员用浏览器管理大屏内容。 |

### 三个核心展示页面

1. **班牌主屏 `/`**：教室大屏主页。**左侧是 Windows 桌面图标区域，任何情况下不得遮挡左侧。**
2. **室外屏 `/outside`**：校门口/走廊等非教室大屏，无左侧遮挡约束。
3. **设备配对页 `/pair`**：大屏首次使用时的配对引导。

---

## 1. 角色与权限

### 1.1 角色定义

| 角色 | 职责 |
| --- | --- |
| **超级管理员 superadmin** | 平台级。管理所有学校/年级/班级、所有账户、系统全局配置、审计日志。 |
| **学校管理员 school** | 管理一个或多个学校。管理校内年级/班级、本校内所有内容、校内下级账户。 |
| **年级管理员 grade** | 管理一个年级（多个班）。管理年级内内容、年级内下级账户。 |
| **班级管理员 class** | 管理一个班级的内容。 |
| **设备主体 device** | 大屏本体。只读，展示分配给它所在班级的内容。 |

### 1.2 账户与班级的绑定（双向）

- 一个账户可以绑定多个班级（一个年级/学校管理员可同时管多个班）。
- 一个班级可以绑定多个账户（多名老师共同管一个班）。
- 规则：
  - 绑定班级 = 该班班级管理员（class 角色）。
  - 绑定年级 = 该年级全部班级的管理权。
  - 绑定学校 = 该校全部班级的管理权。
  - 账户权限 = 其所有绑定的并集。

### 1.3 授权规则（只许向下）

- 高权限可以**创建/授权**低权限账户：
  - 超级管理员 → 可创建学校、年级、班级管理员；
  - 学校管理员 → 可创建自己校内/校级的年级、班级管理员；
  - 年级管理员 → 可创建自己年级内的班级管理员；
  - 班级管理员 → 不能创建任何账户。
- 低权限不能提升自己或他人的权限，不能创建与自己平级或更高级的账户。
- 账户支持：禁用/启用、重置密码、删除。

### 1.4 内容发布与可见规则

公告、倒计时、正计时、课程表、作息等所有内容，发布时都要选择**发布范围**：

- **全部**（发布者管辖范围内的全部班级）
- **指定学校**
- **指定年级**
- **指定班级**（可多选）

可见/可管理规则（核心闭环）：

1. **高权限发布的内容，低权限用户可见但不可管理**——低权限用户在后台能看到这些内容，但只能看，不能改、不能删。
2. 自己发布的内容，自己可以改、删。
3. 高权限可以管理自己范围内由低权限发布的内容。
4. 大屏展示端只按"自己班级"聚合显示所有覆盖到本班的内容，不分来源高低。

---

## 2. 展示端需求（页面细节）

### 2.1 班牌主屏 `/`

#### 布局红线（最高优先级，任何实现都不得违反）

- 页面左侧存在 Windows 桌面图标区域，**任何内容不得出现在左侧、不得遮挡左侧**。
- 顶部横条（时间条、课程表条）可以横跨全屏宽度；其余内容一律放在右侧区域。
- 左侧图标区宽度**可在后台配置**（班级基础设置），**默认值为 0**，即默认不占用左侧、内容铺满全屏。
- 任何弹层/抽屉（如公告详情）只能从右侧弹出或居中显示，**严禁从左侧弹出、严禁覆盖左侧**。

#### 整体视觉

- 整屏为**可配置壁纸背景**（管理员上传的图片或图片链接），铺满、保持比例不拉伸变形。
- 所有内容块呈半透明玻璃质感卡片，白色文字，与大屏背景融合。
- 默认分辨率按 1920×1080 大屏设计；内容自动随屏幕缩放。

#### 页面内容（自上而下，默认全宽横排时图标区在左侧下方，允许横条跨全屏）

**① 顶部信息条**
- 左侧格：**日期**（如 2026/8/15）；**时间**（如 09:41，**每秒刷新**）；**星期/周数**（如 周六 · 第 34 周）。
- 展示端自动获得本班信息，显示班级名（如"高一（1）班"）。

**② 课程表条**
- 显示"今天"的课程，按节次从左到右排布。
- 每个节次格显示课程名；当前上课时间段高亮（醒目色块），前后节次弱化显示。
- 当前时间不在上课时段时，课程条普通显示，不特殊高亮。
- 无课程表或今天无课时，显示友好提示"今日暂无课程"，不报错。

**③ 公告栏（列表 + 详情）**
- 位于右侧区域，列表展示公告**标题**；置顶公告排在最前并有置顶标识。
- 列表项显示：标题 + 简短来源（如"学校管理员发布"）+ 置顶标记。
- **点击标题** → 从**右侧**滑出**详情抽屉**：
  - 抽屉内显示标题 + 富文本正文（可滚动）。
  - 抽屉可从右侧关闭（关闭按钮 / 点击抽屉外区域）。
  - **抽屉只从右侧展开，绝不覆盖左侧图标区。**
- 无公告时该区域隐藏或显示空提示，不影响其他内容。

**④ 倒计时 / 正计时卡片**
- 展示所有覆盖本班的事件，每个事件一行：
  - 倒计时："距【事件】还有 N 天"
  - 正计时："【事件】已进行 N 天"
- N 为 0 或正数时展示；已过期/为负的事件不展示。

**⑤ 天气卡片**
- 当前天气：温度、天气现象、风力/风向、湿度、体感温度。
- 未来 3 天预报：每天日期 + 白天/夜间天气图标 + 最高/最低温。
- 降雨提示：最近降雨概况文字。
- 天气不可用时显示"天气数据暂不可用"，不影响页面。

**⑥ 进度条**
- 今日时间已过百分比（从 6:00 至 22:20 计算），百分比数字 + 进度条展示。

**⑦ Slogan 标语**
- 展示一句话 + 出处（来自外部一言 API）。
- 刷新间隔由管理员在后台配置（秒）。
- 获取失败时显示"今日寄语暂不可用"。

#### 刷新与异常

- 内容每 **60 秒**自动刷新；时间每秒刷新；天气每 60 秒刷新。
- 所有内容为空 / 接口失败时：**页面不报错、不白屏**，保留上次成功数据，缺失部分隐藏。
- 页面长期运行不卡顿；定期（如每日凌晨 2 点）自动重载页面防止累积问题。

---

### 2.2 室外屏 `/outside`

- **无左侧遮挡约束**，布局自由，右对齐排版。
- 内容：课程表条 + 倒计时/正计时列表 + 公告 + 时间/进度 + 天气。
- 公告以**居中弹窗**查看详情（点击标题打开，关闭按钮/点击遮罩关闭）。
- 其余表现与班牌主屏一致（壁纸、半透明卡片、白字、刷新频率、空数据降级）。

---

### 2.3 设备配对页 `/pair`

- 大屏首次使用、或重新配对的引导页。
- 页面内容：
  - 标题"设备配对"，副标题"请输入管理员生成的 8 位配对码"。
  - 输入框：**配对码**（8 位，自动去空格、转大写、限长 8 位）。
  - 输入框：**设备名称**（如"一班班牌"）。
  - 提交按钮"配对设备"。
- 校验：配对码必须为 8 位、设备名称必填，否则提示。
- 提交成功 → 保存配对信息，自动进入班牌主屏（或室外屏，按目标配置）。
- 提交失败 → 页面显示错误提示（如配对码无效、已使用、已删除），不清空输入，可重试。
- 已配对状态下访问本页可重新配对（旧绑定失效）。

---

## 3. 内容模块需求

### 3.1 公告（公告栏）

- 富文本公告：**标题** + **富文本正文**（支持字号/加粗/列表/图片等，字号有上限约束）。
- 发布范围：全部 / 指定学校 / 指定年级 / 指定班级（见 §1.4）。
- 支持：编辑、删除、置顶、上/下线开关、**定时发布**（可选开始/结束时间，未到或已过则不显示）。
- 置顶：多篇置顶时置顶的排最前；取消置顶恢复按时间排序。
- 展示端只显示：**上线中 + 在有效期内 + 覆盖本班** 的公告。

### 3.2 倒计时与正计时

- **倒计时**：事件 + 目标日期 → 展示"距【事件】还有 N 天"。
- **正计时**：事件 + 起始日期 → 展示"【事件】已进行 N 天"。
- 每个事件带发布范围（§1.4）。
- 管理端：增、删、改（事件名/类型/日期/范围）。
- 展示端：聚合所有覆盖本班的事件并展示；过期事件不显示。

### 3.3 课程表

**作息时间配置（作息表）**
- 定义一天有几节课、每节课的起止时间（如第 1 节 08:00–08:45）。
- 一个学校可以有多份作息配置（如夏季/冬季、不同年级差异）。
- 支持：新建、复制、编辑、删除。
- 每个班级选择"使用哪一份"；支持上游（学校/年级管理员）**统一下发**作息配置到多个班级。
- 班级也可以基于上游配置**复制后修改**成自己的专属作息。
- **上游可查看下游班级当前使用的作息配置情况**（哪班用哪份、是否自定义）。

**课程表本体**
- 一个班级可以有多份课程表（如上学期/下学期/临时课表）。
- 支持：新建、复制、编辑、删除、**设为当前使用**（每个班级同时只有一份生效）。
- 两种模式：
  - **周模式**：配置周一至周日 7 天的课程（每天按节次填课）。
  - **单双日模式**：只配置「单日」「双日」两套，系统按当天奇偶自动选择。
- 编辑方式为**在线编辑**（网格填字），不依赖上传文件。
- **上游（学校/年级管理员）统一下发**新课程表到多个班级。
- **上游可查看下游班级课程表配置情况**。
- 展示端：按"今天"解析出课程 + 节次时间，当前节高亮。

---

## 4. 管理端需求（页面细节）

管理端统一视觉：左侧导航 + 顶部栏（登录账户、角色、班级选择器、退出）。列表统一带分页/搜索；所有表单带校验；危险操作用确认框；操作成功/失败用顶部或角落提示条反馈。

### 4.1 登录页

- 页面：居中卡片，标题"后台管理登录"。
- 字段：用户名、密码。
- 提交后校验必填；登录失败提示"用户名或密码错误"，不清空输入。
- 登录成功 → 进入后台首页（按角色默认落地页）；可保存重定向目标（如之前访问被拦的页面）。

### 4.2 后台框架（所有页面共用）

- **顶部栏**：当前登录账户名 + 角色标签（如"学校管理员"）+ 班级选择器（下拉，显示可管理的班级/年级/学校）+ 退出按钮。
- **左侧导航**：按角色显示可访问菜单（无权限的菜单项不显示）。
- **班级选择器**：用于需要针对某个班级操作的功能（基础设置、设备、课程表等）；选择后该页数据跟随切换。
- 退出：二次确认后退出并返回登录页。

### 4.3 机构管理（superadmin / school）

**学校列表**（仅 superadmin）
- 表格：学校名、地区、年级数、班级数、创建时间；操作：编辑、删除。
- 新建/编辑弹窗：学校名（必填）、地区（可空）。
- 删除：需确认；有班级的学校不可删除（提示先删除班级）。

**年级管理**（superadmin / school）
- 按学校查看年级列表：年级号、名称、班级数；操作：编辑、删除。
- 新建：选择学校（superadmin 需选）、输入年级号与名称。

**班级管理**（superadmin / school / grade）
- 按学校/年级筛选查看班级列表：班号、班级名、所属年级、设备数；操作：编辑、删除。
- **批量创建**：选择学校/年级，输入起止班号（如 1–8），一次生成多个班级。
- 新建单个班级：选择学校/年级、班号、班级名。
- 班级可设置：**左侧图标区宽度**（px，默认 0）。
- 删除班级：需确认；有设备的班级提示"请先撤销该班设备"再删。

### 4.4 账户管理（superadmin / school / grade）

- 表格：用户名、显示名、角色、绑定范围（学校/年级/班级名）、状态（启用/禁用）、最近登录；操作：编辑、禁用/启用、重置密码、删除。
- 新建/编辑弹窗：
  - 用户名（必填，唯一）、显示名、初始密码（新建必填）。
  - **角色**（下拉，只能选比当前更低级的角色；班级管理员不可见本页）。
  - **绑定范围**：可多选学校/年级/班级，且只能选当前操作者管辖范围内的（越界选项不可选或提交被拒）。
- 重置密码：弹窗输入新密码（必填）。
- 删除：需确认；删除后该账户立即失效。

### 4.5 公告管理

- 列表：标题、发布范围（如"全部 / 高二年级 / 指定 3 个班"）、来源（发布者 + 角色）、置顶标识、状态（上线中/已下线/定时）、生效时间、操作（编辑/删除/上线下线/置顶）。
- **只读来源**：高权限发布的内容对本账户显示为只读——标"只读（来源：XX管理员）"，隐藏编辑/删除按钮。
- 新建/编辑弹窗：
  - 标题（必填）、富文本正文（必填）。
  - 发布范围：单选"全部 / 指定学校 / 指定年级 / 指定班级"，选具体项时联动出可选项（仅管辖范围内）。
  - 置顶开关、上线开关。
  - 可选定时：开始时间 / 结束时间（可为空）。
- 删除、下线需确认。

### 4.6 倒计时/正计时管理

- 列表：事件名、类型（倒计时/正计时）、日期、发布范围、来源；操作：编辑、删除。
- 新建/编辑弹窗：事件名（必填）、类型单选、日期选择（必填）、发布范围（同公告）。
- 删除需确认。

### 4.7 课程表管理

页面含两个 Tab：**作息配置** 与 **课程表**。

**Tab 1：作息配置**
- 列表：名称、节次数、来源（own/pushed/系统默认）、操作：编辑、复制、删除、下发。
- 新建/编辑：配置名（必填）+ 节次编辑器——按行编辑：节次序号、名称（如"第一节"）、开始时间、结束时间；可增行/删行/上移下移。
- 复制：一键生成副本（可改名）供修改。
- 删除：需确认；正在被班级使用的配置提示"仍可删除，使用班级将退回默认"。
- **下发**：选择目标班级集合 → 确认 → 写入目标班级并设为使用。

**Tab 2：课程表**
- 班级选择器决定操作哪个班（顶栏班级选择器联动）。
- 列表：名称、模式（周/单双日）、来源、是否生效；操作：编辑、复制、删除、**设为使用**、下发。
- 新建：选择模式（周 / 单双日）→ 进入网格编辑器：
  - **周模式**：行=节次（来自该班使用的作息），列=周一~周日，单元格填课程名。
  - **单双日模式**：行=节次，列=单日、双日。
- 网格编辑器：单元格文本输入、可清空；节次行高亮当前时间对应节（可选）。
- 设为使用：同一班级同时只允许一份生效；切换时原生效的自动取消。
- 删除：需确认；被设为使用的课程表删除后该班无生效课表（展示端显示"今日暂无课程"）。
- **下发**：选目标班级集合 → 确认 → 生成副本并设为使用。
- **下游配置情况查看**（上游）：按年级查看每班当前使用的作息与课程表，标记来源（班级自配/上游下发/默认）。

### 4.8 设备管理

**配对码区**
- 选班级 → "生成配对码"按钮 → 弹窗展示一次 8 位配对码 + 所属班级，提示"请立即复制，关闭后不再显示"。
- 配对码列表：班级、生成时间、状态（未使用/已使用）；操作：删除（硬删除，删除后立即失效）。删除需确认。
- 配对码可继续为同一班级生成多条。

**设备列表**
- 表格：设备名、所属班级、最近 IP、最近在线时间、配对时间、状态（正常/已撤销）；操作：编辑（改名/调班）、撤销。
- 编辑弹窗：设备名（必填）、所属班级（下拉，仅管辖范围）。
- 撤销：确认后该设备立即失效，回到配对页可重新配对。

### 4.9 基础设置（班级级）

- 页面顶栏班级选择器选择目标班级。
- 内容：
  - **壁纸**：链接输入 + 保存；或上传图片文件 → 保存后自动设为壁纸。
  - **Slogan 刷新秒数**：数字输入 + 保存。
  - **公告开关**：开启/关闭本班公告栏展示。
  - **左侧图标区宽度**：数字输入（px，默认 0）+ 保存；保存后班牌主屏立即生效。

### 4.10 系统配置（仅 superadmin）

- 表单式页面（非表格）：
  - 天气地区（文本/坐标）。
  - 默认壁纸、默认作息（下拉选择学校内某份作息）、默认图标区宽度、默认 Slogan 秒数。
- 保存成功提示；配置全局生效，各班级可在基础设置中覆盖。

### 4.11 审计日志（仅 superadmin）

- 表格：时间、操作人、动作（如"创建公告""删除账户""修改系统配置"）、详情摘要。
- 支持按时间/操作人/动作筛选。

---

## 5. 全局规则与边界

### 5.1 交互通用规则

- 展示端大屏无操作入口，只读展示。
- 任何删除/撤销/下线等破坏性操作需二次确认。
- 内容为空时页面正常显示，不报错。
- 所有加载有状态反馈（加载中/完成/失败）；失败提示清晰可理解，不出现原始技术错误信息。

### 5.2 数据与隐私

- 密码、配对码、设备令牌等敏感信息不能明文存储、不能出现在任何返回结果或日志中（配对码/令牌只在生成时展示一次）。
- 富文本内容展示前需清理（防脚本注入）。

### 5.3 兼容与稳定性

- 展示端长期运行不卡顿、不白屏；网络异常时保留上次数据，不崩溃。
- 断网/接口失败时页面友好降级。

### 5.4 通用化

- 不绑定单一学校、单一地区。
- 多学校、多年级、多班级并行使用互不干扰。
- 各学校/班级配置相互独立。

---

## 6. 验收标准（红线清单）

1. 班牌主屏左侧图标区**绝不遮挡**；图标区宽度可配置，默认 0。
2. 公告详情只从右侧/居中弹出，不覆盖左侧。
3. 新装系统不自动创建班级；批量建班可用。
4. 账户可双向绑定（账户↔班级）；授权只能向下；越权操作被拒绝。
5. 高权限发布的内容，低权限可见但不可管理（前端隐藏操作 + 后端拒绝）。
6. 课程表：多份配置、可增删改、可设使用；周模式与单双日模式均可用；上游可下发、下游可覆盖、上游可查看下游状态。
7. 公告栏：列表 + 点开详情；定时上/下线生效。
8. 倒计时与正计时都支持并正确展示天数。
9. 展示端在无任何数据/断网时仍能正常显示不崩溃。
10. 敏感信息不出现在任何接口响应与日志中。
