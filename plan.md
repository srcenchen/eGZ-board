# eGZ-Board 产品需求文档（PRD）

> 本文档是唯一权威的产品需求文档。**只讲「产品要做什么、怎么表现、边界和规则」，不涉及任何技术实现、接口或数据库设计**——那些由开发方自行设计。
> 所有功能、行为、权限、布局、验收标准都必须写清楚到可直接开发的程度。

---

## 0. 产品概述

**eGZ-Board 是一套「电子班牌」系统**：教室/走廊/校门口的电子大屏全屏显示信息壁纸，管理员通过网页后台管理大屏上展示的内容。

目标客户：学校（K12 / 高中）。产品需**通用化**，可服务多所学校、多年级、多班级、不同地区，不绑定任何单一学校或地区。

### 两个客户端

| 端 | 说明 |
| --- | --- |
| **展示端**（大屏） | 班级教室内的 Windows 大屏、校门口/走廊竖屏等。开机即全屏展示，无需任何操作。 |
| **管理端**（网页后台） | 学校老师/管理员用浏览器管理大屏内容。 |

### 三个核心页面

1. **班牌主屏 `/`**：教室大屏主页。**注意：左侧是 Windows 桌面图标区域，任何情况下不得遮挡左侧。**
2. **室外屏 `/outside`**：校门口/走廊等非教室大屏，无左侧遮挡约束。
3. **设备配对页 `/pair`**：大屏首次使用时的配对引导。

---

## 1. 角色与权限

### 1.1 角色定义

| 角色 | 职责 |
| --- | --- |
| **超级管理员 superadmin** | 平台级。管理系统、所有学校/年级/班级、所有账户、全局配置。 |
| **学校管理员 school** | 管理一个或多个学校。管理校内年级/班级、本校内所有内容。 |
| **年级管理员 grade** | 管理一个年级（多个班）。管理年级内内容。 |
| **班级管理员 class** | 管理一个班级的内容。 |
| **设备主体 device** | 大屏本体。只读，展示分配给它所在班级的内容。 |

### 1.2 账户与班级的绑定（双向）

- 一个账户可以绑定多个班级（一个年级/学校管理员可同时管多个班）。
- 一个班级可以绑定多个账户（多名老师共同管一个班）。
- 规则：
  - 绑定班级 = 该班班级管理员（class 角色）。
  - 绑定年级 = 该年级全部班级的管理权。
  - 绑定学校 = 该校全部班级的管理权。
  - 账户权限 = 其所有绑定的并集。

### 1.3 授权规则（只许向下）

- 高权限可以**创建/授权**低权限账户：
  - 超级管理员 → 可创建学校、年级、班级管理员；
  - 学校管理员 → 可创建自己校内/校级的年级、班级管理员；
  - 年级管理员 → 可创建自己年级内的班级管理员；
  - 班级管理员 → 不能创建任何账户。
- 低权限不能提升自己或他人的权限，不能创建与自己平级或更高级的账户。
- 账户支持：禁用/启用、重置密码、删除。

### 1.4 内容发布与可见规则

公告、倒计时、正计时、课程表、作息等所有内容，发布时都要选择**发布范围**：

- **全部**（发布者管辖范围内的全部班级）
- **指定学校**
- **指定年级**
- **指定班级**（可多选）

可见/可管理规则（核心闭环）：

1. **高权限发布的内容，低权限用户可见但不可管理**——低权限用户在后台能看到这些内容，但只能看，不能改、不能删。
2. 自己发布的内容，自己可以改、删。
3. 高权限可以管理自己范围内由低权限发布的内容。
4. 大屏展示端只按"自己班级"聚合显示所有覆盖到本班的内容，不分来源高低。

---

## 2. 展示端需求

### 2.1 班牌主屏 `/`

**布局红线（最高优先级）**：

- 页面左侧存在 Windows 桌面图标区域，**任何内容不得出现在左侧、不得遮挡左侧**。
- 顶部的时间条和课程表条可以横跨全屏（图标一般在下方），其余内容一律放在右侧区域。
- 左侧图标区宽度**可在后台配置**（班级基础设置），**默认值为 0**，即默认不占用左侧、内容铺满全屏。
- 任何弹层/抽屉（如公告详情）只能从右侧弹出或居中显示，**严禁从左侧弹出、严禁覆盖左侧**。

页面内容（自上而下）：

1. **顶部信息条**：日期、时间、星期、周数。时间每秒刷新。
2. **课程表条**：显示"今天"的课程；当前上课时间段高亮，前后节次弱化显示。无数据时显示友好提示。
3. **公告栏**：右侧区域列表展示公告标题（置顶的在前）；**点击标题展开详情**（右侧抽屉），详情含富文本正文。列表可滚动。
4. **倒计时/正计时卡片**：展示所有覆盖本班的事件。
5. **天气卡片**：当前天气（温度、天气现象、风力、湿度）+ 未来 3 天预报 + 降雨提示。
6. **进度条**：今日时间已过百分比。
7. **Slogan 标语**：展示一句话及出处（来自外部一言 API）。

其他：

- 整屏为可配置壁纸背景（链接或上传的图片）。
- 内容每 60 秒自动刷新；天气每 60 秒刷新。
- 所有内容为空时页面不报错、不白屏，正常显示其余部分。

### 2.2 室外屏 `/outside`

- 布局自由，无左侧约束。
- 内容：课程表条 + 倒计时/正计时列表 + 公告（可居中弹窗查看详情）+ 时间/进度 + 天气。
- 公告详情可用居中弹窗。

### 2.3 设备配对页 `/pair`

- 大屏首次使用展示。
- 用户输入 **8 位配对码** + **设备名称**，提交成功即完成配对，进入班牌主屏。
- 配对码由管理员在后台为指定班级生成。

---

## 3. 内容模块需求

### 3.1 公告（公告栏）

- 富文本公告（支持标题 + 正文）。
- 发布范围：全部 / 指定学校 / 指定年级 / 指定班级（见 §1.4）。
- 支持：编辑、删除、置顶（多篇置顶时置顶的排最前）、上/下线开关、**定时发布**（可选开始/结束时间，未到或已过则不显示）。
- 展示端只显示：上线中 + 在有效期内 + 覆盖本班 的公告。

### 3.2 倒计时与正计时

- **倒计时**：事件 + 目标日期 → 展示"距【事件】还有 N 天"。
- **正计时**：事件 + 起始日期 → 展示"【事件】已进行 N 天"。
- 每个事件带发布范围（§1.4）。
- 管理端：增、删、改（事件名/类型/日期/范围）。
- 展示端：聚合所有覆盖本班的事件并展示。

### 3.3 课程表

课程表是重头模块，能力必须完整：

**作息时间配置（作息表）**
- 定义一天有几节课、每节课的起止时间（如第 1 节 08:00–08:45）。
- 一个学校可以有多份作息配置（如夏季/冬季、不同年级差异）。
- 支持：新建、复制、编辑、删除。
- 每个班级选择"使用哪一份"；支持上游（学校/年级管理员）**统一下发**作息配置到多个班级。
- 班级也可以基于上游配置**复制后修改**成自己的专属作息。
- **上游可查看下游班级当前使用的作息配置情况**（哪班用哪份、是否自定义）。

**课程表本体**
- 一个班级可以有多份课程表（如上学期/下学期/临时课表）。
- 支持：新建、复制、编辑、删除、**设为当前使用**（每个班级同时只有一份生效）。
- 两种模式：
  - **周模式**：配置周一至周日 7 天的课程（每天按节次填课）。
  - **单双日模式**：只配置「单日」「双日」两套，系统按当天奇偶自动选择。
- 编辑方式为**在线编辑**（网格填字），不依赖上传文件。
- **上游（学校/年级管理员）统一下发**新课程表到多个班级。
- **上游可查看下游班级课程表配置情况**。
- 展示端：按"今天"解析出课程 + 节次时间，当前节高亮。

---

## 4. 管理端需求

### 4.1 登录与导航

- 管理员账号密码登录。
- 登录后按角色显示可访问菜单；无权限入口不显示，越权访问被拒绝。
- 页面含：顶部（登录账户 + 角色 + 班级选择器 + 退出）+ 左侧导航。

### 4.2 机构管理

- 学校、年级、班级的增删改。
- **班级不默认创建**（新系统不含任何预设班级），由管理员创建。
- 支持**批量创建班级**（选学校/年级，输入起止班号，一次生成多个班）。

### 4.3 账户管理

- 创建子账户（选角色 + 绑定范围），编辑、禁用/启用、重置密码、删除。
- 创建账户时只能绑定到自己管辖范围内，且只能创建比自己低级的角色。

### 4.4 公告管理

- 列表 + 新建/编辑弹窗（富文本）。
- 设置：标题、正文、发布范围、置顶、上/下线、定时。
- 只读来源标"只读（来源：XX管理员）"。

### 4.5 倒计时/正计时管理

- 列表 + 新建/编辑：事件名、类型（倒/正计时）、日期、发布范围。

### 4.6 课程表管理

- 两个 Tab：**作息配置** 和 **课程表**。
- 作息配置：列表 + 编辑节次（第几节/名称/开始/结束）+ 复制 + 下发。
- 课程表：列表（含生效标记）+ 在线网格编辑（周模式 7 列 / 单双日模式 2 列）+ 设使用 + 复制 + 删除 + 下发 + 下游配置情况查看。
- 下发：选择目标班级集合，一键下发并设为使用。

### 4.7 设备管理

- 按班级生成配对码（显示一次）、删除配对码（硬删除，删除后失效）。
- 设备列表：改名、调班、撤销（解除配对）。

### 4.8 基础设置（班级级）

- 壁纸（上传或链接）、Slogan 刷新秒数、公告开关、**左侧图标区宽度**。

### 4.9 系统配置（仅超级管理员）

- 天气地区、默认壁纸、默认作息、默认图标区宽度、默认 Slogan 秒数等全局默认值。
- 所有业务配置都在后台完成，不使用配置文件。

### 4.10 审计日志（仅超级管理员）

- 记录关键操作：谁、何时、做了什么（登录、内容增删改、账户变更、配置变更等）。

---

## 5. 全局规则与边界

### 5.1 通用规则

- 展示端大屏无操作入口，只读展示。
- 任何删除操作需二次确认。
- 内容为空时页面正常显示，不报错。

### 5.2 数据与隐私

- 密码、配对码、设备令牌等敏感信息不能明文存储、不能出现在任何返回结果或日志中（配对码/令牌只在生成时展示一次）。
- 富文本内容展示前需清理（防脚本注入）。

### 5.3 兼容与稳定性

- 展示端长期运行不卡顿、不白屏；网络异常时保留上次数据，不崩溃。
- 断网/接口失败时页面友好降级。

### 5.4 通用化

- 不绑定单一学校、单一地区。
- 多学校、多年级、多班级并行使用互不干扰。
- 各学校/班级配置相互独立。

---

## 6. 验收标准（红线清单）

1. 班牌主屏左侧图标区**绝不遮挡**；图标区宽度可配置，默认 0。
2. 公告详情只从右侧/居中弹出，不覆盖左侧。
3. 新装系统不自动创建班级；批量建班可用。
4. 账户可双向绑定（账户↔班级）；授权只能向下；越权操作被拒绝。
5. 高权限发布的内容，低权限可见但不可管理（前端隐藏操作 + 后端拒绝）。
6. 课程表：多份配置、可增删改、可设使用；周模式与单双日模式均可用；上游可下发、下游可覆盖、上游可查看下游状态。
7. 公告栏：列表 + 点开详情；定时上/下线生效。
8. 倒计时与正计时都支持并正确展示天数。
9. 展示端在无任何数据/断网时仍能正常显示不崩溃。
10. 敏感信息不出现在任何接口响应与日志中。
